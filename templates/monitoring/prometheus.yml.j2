{# Human-authored template. The rendered output is generated. #}
# Generated by Medusa. Do not edit manually.
#
# Prometheus main config. Both this file and the sibling prometheus-targets
# file stage into the prometheus service's stack tree (dest prometheus/), so
# the container binds them relatively (./prometheus) at /etc/prometheus/medusa
# -- the path convention this file's scrape_config_files entry assumes.
global:
  scrape_interval: 30s
scrape_config_files:
  - /etc/prometheus/medusa/prometheus-targets.yaml
