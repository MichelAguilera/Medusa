{# Human-authored template. The rendered output is generated. #}
{ modulesPath, ... }:
{
  # Generated by Medusa. Do not edit manually.
  #
  # Installer ISO (T-089): one generic image serves every host -- it carries
  # only the controller key(s), so rebuild only when a key rotates. The
  # embedded nixpkgs snapshot affects only the live environment.
  imports = [ (modulesPath + "/installer/cd-dvd/installation-cd-minimal.nix") ];

  # nixos_installer_keys from dns.yaml -- plain public-key inventory data.
  users.users.root.openssh.authorizedKeys.keys = [
{% for key in nixos.installer_keys %}
    "{{ key }}"
{% endfor %}
  ];

  # Reports the live system's DHCP address to the hypervisor UI (for --ip).
  services.qemuGuest.enable = true;
}
