{# Human-authored template. The rendered output is generated. #}
# Generated by Medusa. Do not edit manually.
# Recipients are derived from the secret->host map (T-080): each rule lists the
# operator keys plus the age recipient of every host that uses that secret.
# This file lives under generated/, so run sops against it explicitly:
#   sops --config generated/.sops.yaml secrets/<path>.sops.yaml
#   find secrets -name '*.sops.yaml' \
#     -exec sops --config generated/.sops.yaml updatekeys -y {} \;
creation_rules:
{% for rule in rules %}
  - path_regex: {{ rule.path_regex }}
    key_groups:
      - age:
{% for recipient in rule.recipients %}
          - {{ recipient }}
{% endfor %}
{% endfor %}
