{# Human-authored template. The rendered output is generated. #}
#!/usr/sbin/nft -f
# Generated by Medusa. Do not edit manually.
# Mark traffic from the tunnel Docker subnet so policy routing can steer it.
# The mark is set in PREROUTING, before Docker's POSTROUTING masquerade, so it
# is keyed on the real container source address. A dedicated table keeps this
# isolated from other firewall rules on the host.

add table inet medusa_tunnel
delete table inet medusa_tunnel

table inet medusa_tunnel {
    chain prerouting {
        type filter hook prerouting priority mangle; policy accept;
        ip saddr {{ egress.tunnel_subnet }} meta mark set {{ egress.fwmark }}
    }
}
