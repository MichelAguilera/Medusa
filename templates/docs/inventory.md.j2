{# Human-authored template. The rendered output is generated. #}
# Generated Inventory

Generated by Medusa. Do not edit manually.

## Hosts

| Host | IP | Zones | DNS Names |
| --- | --- | --- | --- |
{% for host in dns.hosts %}
| `{{ host.name }}` | `{{ host.ip }}` | {% for zone in host.zones %}`{{ zone }}`{% if not loop.last %}, {% endif %}{% endfor %} | {% for fqdn in host.fqdns %}`{{ fqdn }}`{% if not loop.last %}, {% endif %}{% endfor %} |
{% endfor %}

## Services

| Service | Host | Stack |
| --- | --- | --- |
{% for service in services.services %}
| `{{ service.id }}` | `{{ service.host }}` | {% if service.stack %}`{{ service.stack }}`{% endif %} |
{% endfor %}

{% if storage.exports or storage.mounts %}
## Storage

{% if storage.exports %}
### NFS Exports

| Export | Server | Path | Options |
| --- | --- | --- | --- |
{% for export in storage.exports %}
| `{{ export.id }}` | `{{ export.server }}` | `{{ export.path }}` | `{{ export.options | join(',') }}` |
{% endfor %}

{% endif %}
{% if storage.mounts %}
### Mounts

| Mount | Host | Mountpoint | Source | Type | Options |
| --- | --- | --- | --- | --- | --- |
{% for mount in storage.mounts %}
| `{{ mount.id }}` | `{{ mount.host }}` | `{{ mount.mountpoint }}` | `{{ mount.source }}` | `{{ mount.type }}` | `{{ mount.options | join(',') }}` |
{% endfor %}

{% endif %}
{% endif %}
## Generated Outputs

- CoreDNS: `generated/coredns/`
- Homepage: `generated/homepage/<host>/services.yaml`
- Traefik: `generated/traefik/<host>/dynamic.yaml`
- Docker Compose: `generated/compose/`
- Monitoring: `generated/monitoring/<host>/prometheus-targets.yaml`
- Secrets manifest: `generated/secrets-manifest.yaml`
- Storage manifest: `generated/storage-manifest.yaml`
- NFS exports: `generated/storage/exports/<host>/medusa.exports`
