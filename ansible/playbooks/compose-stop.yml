---
- name: Refuse compose dispatch that would act on nothing
  hosts: medusa_control
  gather_facts: false
  roles:
    - compose_guard

- name: Stop Compose stacks
  hosts: docker_hosts
  gather_facts: false
  vars:
    medusa_compose_apply: true
    medusa_compose_state: stopped
  roles:
    - compose
